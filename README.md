First, navigate to the root directory of the project, then, run the back end server with
`mvn org.springframework.boot:spring-boot-maven-plugin:run`

then, start the front-end server with 

``cd frontend``

``npm start``